# Clase 3 – Componentes, Data Binding y Control Flow

## Data Binding
Interpolación, property, event y two-way binding.

## Control Flow moderno
```html
@if (activo) {
  <p>Activo</p>
}
```

```html
@for (item of items; track item) {
  <li>{{ item }}</li>
}
```
